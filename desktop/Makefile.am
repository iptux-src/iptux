desktop = iptux.desktop
logo = ip-penguin.png ip-tux.png
EXTRA_DIST = $(desktop) $(logo)

desktopdir = $(datadir)/applications
desktop_DATA = $(desktop)
logodir = $(datadir)/pixmaps
logo_DATA = $(logo)